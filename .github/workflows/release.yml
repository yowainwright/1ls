name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact: 1ls-qjs-darwin-arm64
          - os: macos-12
            artifact: 1ls-qjs-darwin-x64
          - os: ubuntu-latest
            artifact: 1ls-qjs-linux-x64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install QuickJS (macOS)
        if: runner.os == 'macOS'
        run: brew install quickjs

      - name: Install QuickJS (Linux)
        if: runner.os == 'Linux'
        run: |
          git clone https://github.com/bellard/quickjs.git /tmp/quickjs
          cd /tmp/quickjs
          make -j$(nproc)
          sudo make install

      - name: Install dependencies
        run: bun install

      - name: Build QuickJS bundle
        run: |
          mkdir -p dist/qjs
          bun build ./src/browser/index.ts --outfile dist/qjs/core.js --target browser --minify --format esm
          cp src/qjs/cli.js dist/qjs/
          cp src/qjs/constants.js dist/qjs/
          VERSION=$(node -p "require('./package.json').version")
          sed -i.bak "s/__VERSION__/$VERSION/g" dist/qjs/constants.js && rm dist/qjs/constants.js.bak

      - name: Build QuickJS binary
        run: qjsc -m -o ./dist/${{ matrix.artifact }} dist/qjs/cli.js

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ./dist/${{ matrix.artifact }}

  release:
    needs: build
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./dist

      - name: Flatten artifacts
        run: |
          mv ./dist/1ls-qjs-darwin-arm64/1ls-qjs-darwin-arm64 ./dist/
          mv ./dist/1ls-qjs-darwin-x64/1ls-qjs-darwin-x64 ./dist/
          mv ./dist/1ls-qjs-linux-x64/1ls-qjs-linux-x64 ./dist/
          rm -rf ./dist/1ls-qjs-darwin-arm64/ ./dist/1ls-qjs-darwin-x64/ ./dist/1ls-qjs-linux-x64/

      - name: Calculate checksums and update formula
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"
          SHA_ARM64=$(shasum -a 256 ./dist/1ls-qjs-darwin-arm64 | cut -d ' ' -f 1)
          SHA_X64=$(shasum -a 256 ./dist/1ls-qjs-darwin-x64 | cut -d ' ' -f 1)
          SHA_LINUX=$(shasum -a 256 ./dist/1ls-qjs-linux-x64 | cut -d ' ' -f 1)

          sed -i '' "s/version \".*\"/version \"${VERSION}\"/" Formula/1ls.rb
          sed -i '' "s/sha256 \".*\" # arm64/sha256 \"${SHA_ARM64}\" # arm64/" Formula/1ls.rb
          sed -i '' "s/sha256 \".*\" # x64/sha256 \"${SHA_X64}\" # x64/" Formula/1ls.rb
          sed -i '' "s/sha256 \".*\" # linux/sha256 \"${SHA_LINUX}\" # linux/" Formula/1ls.rb

      - name: Push formula to tap repo
        env:
          GH_TOKEN: ${{ secrets.PR_CREATE_TOKEN }}
        run: |
          git clone https://x-access-token:${GH_TOKEN}@github.com/yowainwright/homebrew-tap.git ../tap || git init ../tap
          cd ../tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p Formula
          cp ${{ github.workspace }}/Formula/1ls.rb Formula/1ls.rb
          git add Formula/1ls.rb
          git commit -m "chore: update 1ls formula to ${{ github.ref_name }}" || echo "No changes"
          git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/yowainwright/homebrew-tap.git 2>/dev/null || git remote add origin https://x-access-token:${GH_TOKEN}@github.com/yowainwright/homebrew-tap.git
          git push -u origin main || git push -u origin HEAD:main

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./dist/1ls-qjs-darwin-arm64
            ./dist/1ls-qjs-darwin-x64
            ./dist/1ls-qjs-linux-x64
          generate_release_notes: true
          body: |
            ## Install

            ### Homebrew (macOS/Linux)
            ```bash
            brew install yowainwright/tap/1ls
            ```

            ### npm/bun
            ```bash
            npm install -g 1ls
            ```

            ### curl
            ```bash
            curl -fsSL https://raw.githubusercontent.com/yowainwright/1ls/main/scripts/install.sh | bash
            ```

            ### Binary Downloads
            - `1ls-qjs-darwin-arm64` - macOS Apple Silicon
            - `1ls-qjs-darwin-x64` - macOS Intel
            - `1ls-qjs-linux-x64` - Linux x64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
