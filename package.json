{
  "name": "1ls",
  "version": "0.0.2",
  "description": "1 line script - Lightweight JSON CLI with JavaScript syntax",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "1ls": "./dist/index.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "files": [
    "dist"
  ],
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "bun run ./src/cli/index.ts",
    "build": "bun run clean && bun build ./src/cli/index.ts --outdir dist --target bun --minify && tsc --emitDeclarationOnly --outDir dist && cp -r src/completions dist/",
    "build:binary:all": "bun run clean && mkdir -p bin && bun build ./src/cli/index.ts --compile --minify --target=bun-darwin-arm64 --outfile bin/1ls-darwin-arm64 && bun build ./src/cli/index.ts --compile --minify --target=bun-darwin-x64 --outfile bin/1ls-darwin-x64 && bun build ./src/cli/index.ts --compile --minify --target=bun-linux-x64 --outfile bin/1ls-linux-x64 && bun build ./src/cli/index.ts --compile --minify --target=bun-linux-arm64 --outfile bin/1ls-linux-arm64",
    "clean": "rm -rf dist bin",
    "prepublishOnly": "bun test && bun run build",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=lcov",
    "lint": "bunx oxlint src/",
    "typecheck": "tsc --noEmit",
    "release": "release-it",
    "prepare": "bash scripts/setup-hooks.sh"
  },
  "keywords": [
    "json",
    "cli",
    "javascript",
    "bun",
    "fx",
    "jq"
  ],
  "author": "Jeff Wainwright <https://jeffry.in> (yowainwright@gmail.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yowainwright/1ls.git"
  },
  "bugs": {
    "url": "https://github.com/yowainwright/1ls/issues"
  },
  "homepage": "https://github.com/yowainwright/1ls",
  "devDependencies": {
    "@types/bun": "latest",
    "release-it": "^19.0.5",
    "turbo": "^2.3.3",
    "typescript": "5.9.2"
  },
  "packageManager": "bun@1.1.38"
}
