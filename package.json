{
  "name": "1ls",
  "version": "0.0.2",
  "description": "1 line script - Lightweight JSON CLI with JavaScript syntax",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "1ls": "./dist/index.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run clean && bun build ./src/cli/index.ts --outdir dist --target bun --minify && tsc --emitDeclarationOnly --outDir dist",
    "build:binary": "bun run clean && mkdir -p bin && bun build ./src/cli/index.ts --compile --minify --outfile bin/1ls",
    "build:binary:macos": "bun run clean && mkdir -p bin && bun build ./src/cli/index.ts --compile --minify --target=bun-darwin-arm64 --outfile bin/1ls-darwin-arm64 && bun build ./src/cli/index.ts --compile --minify --target=bun-darwin-x64 --outfile bin/1ls-darwin-x64",
    "build:binary:linux": "bun run clean && mkdir -p bin && bun build ./src/cli/index.ts --compile --minify --target=bun-linux-x64 --outfile bin/1ls-linux-x64 && bun build ./src/cli/index.ts --compile --minify --target=bun-linux-arm64 --outfile bin/1ls-linux-arm64",
    "build:binary:all": "bun run build:binary:macos && bun run build:binary:linux",
    "clean": "rm -rf dist bin",
    "dev": "bun run ./src/cli/index.ts",
    "prepublishOnly": "bun run test && bun run build",
    "test": "bun test",
    "test:unit": "bun test test/unit/",
    "test:integration": "bun test test/integration/",
    "test:integration:docker": "cd test/integration && docker-compose up --build --abort-on-container-exit",
    "test:integration:docker:clean": "cd test/integration && docker-compose down -v",
    "typecheck": "tsc --noEmit",
    "lint": "bunx oxlint src/",
    "lint:fix": "bunx oxlint src/ --fix",
    "format": "bunx prettier --check 'src/**/*.{ts,tsx,js,jsx,json}'",
    "format:fix": "bunx prettier --write 'src/**/*.{ts,tsx,js,jsx,json}'",
    "release": "release-it",
    "prepare": "husky"
  },
  "keywords": [
    "json",
    "cli",
    "javascript",
    "bun",
    "fx",
    "jq"
  ],
  "author": "Jeff Wainwright <https://jeffry.in> (yowainwright@gmail.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yowainwright/1ls.git"
  },
  "bugs": {
    "url": "https://github.com/yowainwright/1ls/issues"
  },
  "homepage": "https://github.com/yowainwright/1ls",
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.1",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "release-it": "^19.0.5",
    "turbo": "^2.3.3",
    "typescript": "5.9.2"
  },
  "packageManager": "bun@1.1.38"
}
