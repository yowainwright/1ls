[server]
host = "0.0.0.0"
port = 8080
workers = 4

[database]
host = "localhost"
port = 5432
name = "myapp"
user = "admin"
max_connections = 50

[logging]
level = "info"
format = "json"
output = "/var/log/app.log"
